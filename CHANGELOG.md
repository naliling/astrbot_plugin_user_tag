# 更新日志

所有对用户可感知的变更都记录在这里。

## v1.4.5（2026-09-25）

一轮「把每条指令和每句发出去的话都真跑一遍」的体检结果，全部有回归测试卡住。

### 修复

- **模型把标签写成 `<Auto_Relation>` 就直接漏给用户**：三处剥标记的正则按大小写敏感匹配，
  写歪的标签既不落库也不被剥掉，用户会收到一条带着 `<Auto_Relation>闺蜜</Auto_Relation>` 的
  消息。现在标记匹配与「有没有标记」的守卫全都不分大小写。
- **自动认定会覆盖用户自己设的关系**：用户设了「朋友」，模型某次回复带了
  `<auto_relation>恋人</auto_relation>`，关系当场被改成恋人、来源也从「自己设置」变成
  「AI 自动认定」。现在自动认定只填空白：用户自设的、管理员代管的、已认过一次的一律不改
  （标记照样剥干净，只是不落库、不发那句「我把你们的关系认定为…」）。
- **自动认定采纳了候选名单之外的关系**：递给模型的名单只含非演绎向，落库却只看「是不是
  预设」，模型选「主人」（主仆·演绎向）也会被全盘采纳。现在与名单同一口径：演绎向不认。
- **「清除关系」的封禁没落盘**：本来就没设过关系的人发一句清除，「不再自动认定」只活在
  内存里，重启后他又会被自动认定一遍。现在这份拒绝跟着写盘。
- **认不出的数据形状会把文件清空**：数据文件是合法 JSON 但不是本插件认识的格式（例如
  `{"version": 1}`）时，旧代码走「旧格式迁移」把空结构写回原文件——既不报错也不留底。
  现在与坏 JSON 同处理：备份成 `user_tag.unknown.json`，再从空数据启动。

### 变更

- 认定成功的通知句不再写「想换发「设置关系 名字」」——照抄就得多出一个名叫「名字」的
  关系，现在给的是具体示例；分类列表与关键词搜索结尾的例子也换成了本分类里的真名字。
- 「关系列表」搜不到时把十个分类名一并报出来（输「奇」的人想找的是「奇幻」那一类）；
  「没有关系记录」补成了带下一步的整句；开了默认关系时，清除后会说明接下来按哪条对待。

## v1.4.4（2026-09-25）

### 修复

- **手动「设置关系」后封住自动认定**：之前只有「清除关系」会封住后续自动认定，手动设置过关系后如果关系再被丢失（如数据目录被清空/重装），同一用户还会重新走自动认定。现在显式设置（自己/管理员代管）即视为该用户关系已定，与「清除关系」对称封住。
- **修「已解除」提示每条消息重复注入**：用户清除关系后，补发的「已解除」提示会被算作最后一条提示、导致之后每条消息都再注一遍，历史越堆越长。现在只补发一次（最后一条提示已是「已解除」就跳过）。

### 变更

- **自动认定触发日志升为 warning**：带 bot/uid/轮次，并提示排查多实例安装；启动时额外打印实例名与数据文件路径（多个实例同时加载时一目了然）；数据文件不存在（目录被清空/换路径）时打 warning，提示「设置过的关系会丢、自动认定会重新触发」。

## v1.4.3（2026-09-21）

### 修复

- **修「<auto_relation> 标签随回复发出去」**：之前自动认定关系时，模型输出的 `<auto_relation>群友</auto_relation>` 有时会跟在消息末尾一起发给用户。根因：旧版只在 `on_decorating_result` 里剥标签，而①流式回复时 token 边生成边发、剥离来不及；②存会话历史存的是带标签的原文（早于 decorating），下一轮模型会照抄历史里的标签。
- **三层剥离兼底**：①新增 `on_llm_response` 钩子在最早处改 `completion_text`（同时净化发出的与存历史的）；②`on_decorating_result` 保留作为非流式兼底；③`on_llm_request` 时把历史上下文里残留的旧标签擦掉，阻断复发。
- **标签剥离加固**：完整标签、只有开标签未闭合（流式截断）、残留裸标签三种都能剥干净。
- **不再抹掉图片/At**：`on_decorating_result` 旧写法把整条消息链换成一个纯文本 Plain，会干掉图片、@ 等组件；现只重写文字、保留其它组件。

## v1.4.2（2026-09-19）

### 新增

- **管理员代管**：`设置关系 @某人 恋人`、`清除关系 @某人`，管理员可以直接帮其他用户设定/取消关系；非管理员带 @ 会被明确拒绝。用户自己设置关系的方式完全不变。
- **AI 自动认定关系**：用户没有设置关系时，聊满一定轮数（默认 12）后，AI 会根据聊天内容从预设关系（仅非演绎向，共 70 条）里自动认定最贴切的一段，并在回复末尾告知。防打扰设计：
  - 每用户最多尝试 3 次，判断不出来不会反复问；
  - 认定成功或用户主动「清除关系」后不再自动认定；
  - 认定提示按轮次号去重，不会重复注入撑大历史；
  - 启用「默认关系」时不会触发；整条链路可一键关闭。
- **会话黑白名单**：新配置 `session_filter_mode`（off / whitelist / blacklist）+ `session_list`（群号或私聊 QQ 号），可以按群/私聊开关本插件。名单外指令回复「本会话未启用关系功能」，注入静默跳过。
- **关系来源**：`我的关系` 现在会显示关系是谁设的——自己设置 / 管理员设置 / AI 自动认定。
- 新增 6 个配置项：`session_filter_mode`、`session_list`、`auto_relation_enabled`、`auto_relation_after_messages`、`auto_relation_max_attempts`、`auto_relation_notify`。

### 变更

- 数据文件升级为 v2 格式（`relations` / `src` / `auto` 三段），启动时自动迁移旧格式，不丢数据。
- `清除关系` 现在同时表示「明确拒绝 AI 自动认定」，之后不会再被自动认定。

## v1.4.0

### 变更

- **注入去重，长对话大幅省 token**：旧版每发一句就往历史里多存一份关系提示；v1.4.0 改为扫描会话历史，本会话已带过这条提示就跳过，历史被截断时自动补注入。实测单条提示 181 字，30 轮对话的关系提示开销从约 1710 字降到 181 字（详见 README「Token 消耗」）。
- 每个用户按机器人独立存储（`{"机器人ID": {"QQ": "关系"}}`），同一个 QQ 面对不同 bot 可以是不同关系。

### 新增

- 132 种预设关系分 10 类（日常/亲友/恋爱/心动/恋爱设定/身份/服务/主仆/奇幻/玩梗），62 条角色扮演向关系单独标注「演绎向」。
- 支持任意自定义关系名（最长 24 字）。
- `关系列表` 支持分类浏览与关键词搜索，`关系详情` 可直接查看模型实际收到的注入内容。
- 管理员指令：`查看所有关系`、`关系统计`。
- 数据文件损坏时自动备份为 `user_tag.bad.json` 并从空数据启动，不会让插件崩掉。

## v1.3.6 及更早

- 初始版本：设置/清除/查看关系，LLM 请求时注入关系提示（每条消息重复注入，v1.4.0 已优化）。
- 旧版数据格式 `{"QQ": "关系"}`，v1.4.0 起自动迁移。

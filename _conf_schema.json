{
  "admin_qq": {
    "description": "管理员QQ列表",
    "type": "list",
    "hint": "这里填写的 QQ 号可以使用「查看所有关系」「关系统计」。AstrBot 全局配置里的管理员（admins_id）无需填写也自动拥有权限。",
    "default": []
  },

  "default_relation": {
    "description": "默认关系名称",
    "hint": "用户没有自己设置关系时按这一条对待。可填预设名（如 朋友、恋人、魅魔），也可填任意自定义关系，最长 24 字。",
    "type": "string",
    "default": "朋友"
  },

  "enable_default_relation": {
    "description": "是否启用默认关系",
    "hint": "关闭时，没设置过关系的用户不会被注入任何内容（一个字都不占）；开启后所有用户都会先按默认关系相处。",
    "type": "bool",
    "default": false
  }
}
